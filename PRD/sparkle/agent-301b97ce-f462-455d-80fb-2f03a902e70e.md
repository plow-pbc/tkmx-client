# Progress Update as of 2026-08-31 07:05 Pacific
*(Most recent updates at top)*

### Summary of changes since last update
Rebased this branch from 198 commits behind `origin/main` onto current `main`, then landed the
`bd init` beads scaffolding as PR #80 after clearing two rounds of roborev FAIL findings. No
product source is touched; the change is tooling, agent instructions, and ignore rules only.

### Detail of changes made:
- Rebase hit a single add/add conflict on `.gitignore`; resolved as a union of the existing
  repo entries and the beads block.
- Removed a stray `.beads/hooks/post-commit.bak-<ts>` backup that `bd init` had committed.
- roborev job 71775 (FAIL) raised three issues, all fixed rather than bypassed:
  `.sparkle/` was missing from `.gitignore` (Sparkle writes `merge-policy.json` into every agent
  worktree, so `git status` was permanently dirty and worker spin-down refused); a project-level
  `.claude/settings.json` added a second, unguarded `bd prime --hook-json` SessionStart hook on
  top of the already-guarded global one, double-injecting the prime block every session and
  failing on machines without `bd` on PATH — dropped, since `CLAUDE.md`/`AGENTS.md` already point
  collaborators at `bd prime`; and `*.db`, `.beads-credential-key`, `.beads/proxieddb/` were
  already covered by the tracked `.beads/.gitignore`.
- roborev job 71779 (FAIL) on the amended commit raised two more: `.beads/interactions.jsonl` was
  tracked but is appended to on every issue mutation — the same dirty-worktree failure the
  `.sparkle/` entry had just fixed, and its audit prose carries local absolute paths into what
  this repo's own `.gitignore` documents as a public repo. Untracked and ignored. The speculative
  root-level `.dolt/` entry was dropped: `bd doctor` confirms the database lives at
  `.beads/embeddeddolt/`, already ignored by `.beads/.gitignore:3`.
- Verification: CI on PR #80 is green (`typecheck + test`, CodeQL javascript-typescript, CodeQL
  actions). The suite cannot be run locally — `npm install` fails compiling the native
  `better-sqlite3@11.10.0` addon against this machine's Node (`no member named 'GetPrototype' in
  'v8::Object'`), a pre-existing environment problem unrelated to this change.

### Beads activity:
- None. No issues were opened or closed; this branch only installs the tracker itself.

### Potential concerns to address:
- `better-sqlite3@11.10.0` will not build against current Node, so no agent can run the test
  suite locally and every change defers verification to CI. Worth pinning a supported Node
  (`engines` + `.nvmrc`) or bumping to a release with prebuilt binaries.
- `bd init` generated all three of the defects roborev caught (stray `.bak` file, duplicate
  unguarded SessionStart hook, tracked runtime-mutated `interactions.jsonl`). Any future repo
  initialized the same way starts with the same problems.
- PR #80 is merge-protected: `plow-pbc/tkmx-client` is pinned in
  `apps/desktop/shared/merge-protected-repos.json`, so the merge is a human action.
