## Progress Update as of 2026-09-01 (Pacific)
*(Most recent updates at top)*

### Summary of changes since last update
Acted on the two roborev findings against `48906ea`, both in `.beads/README.md`.
The install snippet no longer writes to a predictable `/tmp` path or swallows a
404, and the doc's own "Learn More" links now point at the repo the pinned SHA
actually lives in.

### Detail of changes made:
- Install snippet is now `f=$(mktemp) && curl -fsSL <pinned-url> -o "$f" && sh "$f"`.
  Two problems closed at once: the old fixed `/tmp/beads-install.sh` is
  pre-plantable on a shared host, and `curl` without `-f` exits 0 on a 404 with
  GitHub raw's error page as the body — which the next line would have fed to
  `sh`.
- Replaced the remaining `steveyegge/beads` links with `gastownhall/beads`. The
  point of pinning a SHA is that a reader can audit it; links pointing at a repo
  where the commit may not resolve give that away.
- The earlier review job for this commit (73773) had failed in 3s with no review
  produced; re-running it as job 73865 is what surfaced both findings.

### Follow-up in this same entry
Two more roborev findings, on `7a367b4` itself:
- The installer comment still promised the file "can be read before it is run",
  which the `&& sh "$f"` one-liner does not do. Reworded to describe what the
  snippet actually gives you (pinned SHA, `-f` on 404, unpredictable temp path)
  and to say how to split it if you do want to read it first.
- Prepending this entry clobbered the previous entry's
  `## Progress Update as of 2026-08-31 (Pacific)` heading, orphaning its
  sections under the new one. Heading restored.

### Beads activity:
- None. Still no bead claimed; no task assigned to this agent.

### Potential concerns to address:
- PR #95 remains open with all four checks green and `mergeStateStatus: CLEAN`.
  It is waiting on a human: this repo is pinned merge-protected, so no agent
  merge is possible here by policy.

## Progress Update as of 2026-08-31 (Pacific)
*(Most recent updates at top)*

### Summary of changes since last update
Brought this worktree's branch current with `origin/main` (it was 198 commits
behind) and hardened the `bd init` scaffolding before proposing it for `main`.
No product code changed; this branch carries tracker/agent scaffolding only.

### Detail of changes made:
- Rebased `sparkle/agent-d7354a70-...` onto `origin/main`; resolved the add/add
  `.gitignore` conflict by taking main's file and appending the beads block.
- Untracked the `bd init` artifacts that embed absolute machine-local home
  paths (`.beads/hooks/`, `.beads/interactions.jsonl`) and deleted the stray
  `post-commit.bak-<ts>` file. This repo is public, so those must never land.
  `.beads/metadata.json` stays TRACKED - `project_id`/`dolt_database` are the
  shared identity needed for `refs/dolt/data` sync.
- Pinned `.beads/README.md`'s installer from a `curl | bash` off upstream
  `main` to a specific SHA (`d530cdd`), downloaded to a file first, so a reader
  is not piping a moving target into a shell.
- Verified no secrets or home paths remain in the tracked diff vs `origin/main`;
  `.beads/config.yaml` contains comments only.

### Beads activity:
- None. No bead was claimed; no task was assigned to this agent this session.

### Potential concerns to address:
- Whether beads scaffolding belongs on `main` at all is a human call. The PR is
  opened for review, not for an agent to merge - this repo is merge-protected
  for agents.
- The suite has a known red baseline (~32 failures on `main` locally), so a red
  run on this branch is pre-existing, not caused by it.
